[tox]
envlist = py27, py3, flake8

[testenv]
commands = {envbindir}/nosetests --with-coverage --cover-package gopad -v
deps = -r{toxinidir}/dev-requirements.txt

[testenv:flake8]
commands = flake8 setup.py gopad tests
deps = flake8
